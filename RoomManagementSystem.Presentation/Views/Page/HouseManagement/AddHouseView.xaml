<UserControl x:Class="RoomManagementSystem.Presentation.Views.Page.HouseManagement.AddHouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="370" d:DesignWidth="535"
             Width="535" 
             Height="370"
             FontFamily="Roboto">

    <UserControl.Effect>
        <DropShadowEffect ShadowDepth="1" BlurRadius="5" Color="Black" Opacity="0.14"/>
    </UserControl.Effect>

    <Border Background="White" CornerRadius="14">
        <Grid>
            <!-- Nút đóng -->
            <Button Command="{Binding CancelAddHouseCommand}" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Path Data="M 0,0 L 12,12 M 0,12 L 12,0" Stroke="#565656" StrokeThickness="1.5"/>
            </Button>

            <StackPanel Margin="40">
                <TextBlock Text="Thêm thông tin nhà" FontSize="20" Foreground="#8A8A8A" Margin="0,0,0,25"/>

                <!-- Ô NHẬP ĐỊA CHỈ -->
                <TextBlock Text="Địa chỉ *" FontSize="13" Foreground="#292D32" Margin="0,0,0,8"/>
                <Border Background="#F9FBFF" BorderBrush="#F0EEEE" BorderThickness="1" CornerRadius="9.5" Height="36">
                    <Grid>
                        <TextBox Text="{Binding NewHouseAddress, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Padding="15,0,40,0" VerticalContentAlignment="Center" FontSize="12"/>
                        <Button Grid.Column="1" Width="23" Height="23" Margin="0,0,10,0" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <!--        <Image Source="../../../Resources/Images/map_icon.png" Width="23" Height="23"/>  -->
                        </Button>
                    </Grid>
                </Border>

                <!-- Ô NHẬP GHI CHÚ -->
                <TextBlock Text="Ghi chú" FontSize="13" Foreground="#292D32" Margin="0,20,0,8"/>
                <Border Background="#F9FBFF" BorderBrush="#F0EEEE" BorderThickness="1" CornerRadius="9.5" Height="68">
                    <TextBox Text="{Binding NewHouseNotes, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Padding="15,10" FontSize="12" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                    <!-- NÚT THÊM -->
                    <Button Content="Thêm" Command="{Binding SaveNewHouseCommand}" FontSize="14" FontWeight="Medium" Foreground="White" Background="#5A67BA" Width="80" Padding="0,8">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>