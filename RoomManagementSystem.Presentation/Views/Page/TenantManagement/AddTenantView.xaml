<UserControl x:Class="RoomManagementSystem.Presentation.Views.Page.TenantManagement.AddTenantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="535" Height="362"
             FontFamily="Roboto">

    <UserControl.Effect>
        <DropShadowEffect ShadowDepth="1" BlurRadius="5" Color="Black" Opacity="0.14"/>
    </UserControl.Effect>

    <UserControl.Resources>
        <Style x:Key="CustomDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#F9FBFF"/>
            <Setter Property="BorderBrush" Value="#F0EEEE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="9.5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0" Padding="{TemplateBinding Padding}" BorderThickness="0" Background="Transparent" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" Text="{Binding Path=SelectedDate, StringFormat='dd/MM/yyyy', RelativeSource={RelativeSource TemplatedParent}}"/>
                                <Button x:Name="PART_Button" Grid.Column="1" Margin="0,0,10,0" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <TextBlock x:Name="Icon" Text="📅" Foreground="#8A8A8A" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" PlacementTarget="{Binding ElementName=PART_TextBox}" StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Icon" Property="Foreground" Value="#5A67BA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="14">
        <Grid>
            <!-- Nút đóng - SỬA LẠI COMMAND -->
            <Button Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Background="Transparent" BorderThickness="0" Cursor="Hand"
                    Command="{Binding CancelAddCommand}">
                <Path Data="M 0,0 L 12,12 M 0,12 L 12,0" Stroke="#565656" StrokeThickness="1.5"/>
            </Button>

            <StackPanel Margin="40,40,40,40">
                <TextBlock Text="Thêm thông tin người thuê" FontSize="20" Foreground="#8A8A8A" Margin="0,0,0,25"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Cột trái -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Tên" FontSize="13" Foreground="#292D32" Margin="0,0,0,8"/>
                        <!-- SỬA LẠI BINDING -->
                        <TextBox Padding="15,10" BorderThickness="1" BorderBrush="#F0EEEE" Background="#F9FBFF" Text="{Binding NewTenantName, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="9.5"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>

                        <TextBlock Text="Số điện thoại" FontSize="13" Foreground="#292D32" Margin="0,20,0,8"/>
                        <Grid>
                            <!-- SỬA LẠI BINDING -->
                            <TextBox Padding="50,10,15,10" BorderThickness="1" BorderBrush="#F0EEEE" Background="#F9FBFF" Text="{Binding NewTenantPhone, UpdateSourceTrigger=PropertyChanged}">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="9.5"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                            <Border Width="34" Height="28" Background="#F0F0F0" CornerRadius="9" HorizontalAlignment="Left" Margin="6,0,0,0">
                                <TextBlock Text="VN" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </StackPanel>

                    <!-- Cột phải -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Địa chỉ Email" FontSize="13" Foreground="#292D32" Margin="0,0,0,8"/>
                        <!-- SỬA LẠI BINDING -->
                        <TextBox Padding="15,10" BorderThickness="1" BorderBrush="#F0EEEE" Background="#F9FBFF" Text="{Binding NewTenantEmail, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="9.5"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>

                        <TextBlock Text="Ngày bắt đầu thuê" FontSize="13" Foreground="#292D32" Margin="0,20,0,8"/>
                        <!-- SỬA LẠI BINDING -->
                        <DatePicker Style="{StaticResource CustomDatePickerStyle}" SelectedDate="{Binding NewTenantStartDate, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                    <!-- Nút Thêm - SỬA LẠI COMMAND -->
                    <Button Content="Thêm" FontSize="14" FontWeight="Medium" Foreground="White" Background="#5A67BA" Width="80" Padding="0,8"
                            Command="{Binding SaveNewTenantCommand}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>