<UserControl x:Class="RoomManagementSystem.Presentation.Views.Page.TenantManagement.DeleteConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="212" d:DesignWidth="446"
             Width="446"
             Height="212"
             Background="Transparent">

    <!-- Đưa Resources lên đầu để sửa lỗi "could not be resolved" -->
    <UserControl.Resources>
        <!-- Style cho nút đóng (dấu X) - Cập nhật theo SVG -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="4">
                            <Path Data="M6 18L18 6M6 6L18 18"
                                  Stroke="Black"
                                  Opacity="0.34"
                                  StrokeThickness="1.5"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Stretch="Uniform"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho nút Xác nhận - Cập nhật theo SVG -->
        <Style x:Key="ConfirmButtonStyle" TargetType="Button">
            <!-- Màu nền là #E00000 với opacity 6% (0.06), tương đương #0FE00000 -->
            <Setter Property="Background" Value="#0FE00000"/>
            <Setter Property="Foreground" Value="#E00000"/>
            <Setter Property="FontFamily" Value="Roboto"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border CornerRadius="14" Background="White">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="1" Direction="315" Color="Black" Opacity="0.14" BlurRadius="5.4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Close Button (acts as Cancel) -->
            <Button Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,15,15,0" Background="Transparent" BorderThickness="0" Cursor="Hand"
                    Command="{Binding CancelDeleteCommand}">
                <Path Data="M 0,0 L 12,12 M 0,12 L 12,0" Stroke="#565656" StrokeThickness="1.5"/>
            </Button>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="Xóa người thuê" FontSize="20" Foreground="#8A8A8A" Margin="27,35,0,0"/>

            <!-- Content -->
            <TextBlock Grid.Row="1" Text="Bạn có muốn xóa những người dùng này không?" FontSize="13" Foreground="#6F6F6F" HorizontalAlignment="Center" VerticalAlignment="Center"/>


            

            <!-- Confirm Button -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,30,25">
                <Button Content="Xác nhận" Width="88" Height="37"
                        Style="{StaticResource ConfirmButtonStyle}"
                        Command="{Binding ConfirmDeleteCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>