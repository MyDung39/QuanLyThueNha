<UserControl x:Class="RoomManagementSystem.Presentation.Views.Page.ContractManagement.NotificationContractView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="633" d:DesignWidth="877"
             FontFamily="Roboto"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Foreground" Value="#667085"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0,1,0,1"/>
            <Setter Property="BorderBrush" Value="#F0F1F3"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Foreground" Value="#333843"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CenterAllText" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="37">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="LỊCH SỬ THÔNG BÁO HỢP ĐỒNG" Style="{StaticResource HeaderTextStyle}"/>

        <!-- BINDING ItemsSource VÀO "NotificationList" -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding NotificationList}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Background="White" BorderThickness="0"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#E0E2E7"
                  RowHeaderWidth="0" CanUserAddRows="False">
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="MinHeight" Value="40"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}"/>
            </DataGrid.ColumnHeaderStyle>

            <!-- SỬA LẠI CÁC CỘT CHO KHỚP VỚI NotificationItemViewModel -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="STT" Binding="{Binding Stt}" Width="60" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>

                <DataGridTextColumn Header="LOẠI THÔNG BÁO" Binding="{Binding LoaiThongBao}" Width="160" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>

                <DataGridTextColumn Header="NGƯỜI LIÊN QUAN" Binding="{Binding NguoiLienQuan}" Width="160" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>

                <DataGridTextColumn Header="NGÀY" Binding="{Binding NgayThongBao, StringFormat='dd/MM/yyyy HH:mm'}" Width="150" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>

                <!-- Cột Nội dung sẽ chiếm hết không gian còn lại -->
                <DataGridTextColumn Header="NỘI DUNG" Binding="{Binding NoiDung}" Width="*" CellStyle="{StaticResource DataGridCellStyle}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="5"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>