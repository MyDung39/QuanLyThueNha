<UserControl x:Class="RoomManagementSystem.Presentation.Views.Page.ContractManagement.NotificationContractView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="633" d:DesignWidth="877"
             FontFamily="Roboto"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#667085"/>
        </Style>

        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Foreground" Value="#667085"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0,1,0,1"/>
            <Setter Property="BorderBrush" Value="#F0F1F3"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Foreground" Value="#333843"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CenterAllText" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="37">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="LỊCH SỬ THÔNG BÁO HỢP ĐỒNG" Style="{StaticResource HeaderTextStyle}"/>

        <DataGrid x:Name="notificationDataGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="White" BorderThickness="0"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#E0E2E7"
                  AlternationCount="2"
                  AlternatingRowBackground="#FAFAFA"
                  RowHeaderWidth="0"
                  CanUserAddRows="False"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  EnableColumnVirtualization="True">
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="MinHeight" Value="40"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="80" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>
                <DataGridTextColumn Header="LOẠI THÔNG BÁO" Binding="{Binding LoaiThongBao}" Width="260" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>
                <DataGridTextColumn Header="NGƯỜI THUÊ" Binding="{Binding NguoiThue}" Width="200" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>
                <DataGridTextColumn Header="PHÒNG" Binding="{Binding Phong}" Width="160" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>
                <DataGridTextColumn Header="BẮT ĐẦU" Binding="{Binding BatDau}" Width="140" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>
                <DataGridTextColumn Header="KẾT THÚC" Binding="{Binding KetThuc}" Width="140" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>
                <DataGridTextColumn Header="TIỀN CỌC" Binding="{Binding TienCoc}" Width="160" CellStyle="{StaticResource DataGridCellStyle}" ElementStyle="{StaticResource CenterAllText}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
