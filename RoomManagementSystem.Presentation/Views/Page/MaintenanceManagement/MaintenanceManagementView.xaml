<UserControl x:Class="RoomManagementSystem.Presentation.Views.Page.MaintenanceManagement.MaintenanceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RoomManagementSystem.Presentation.Views.Page.MaintenanceManagement"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F7F8FC"
             FontFamily="Roboto"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#EEEEEE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <Image Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Width="16" Height="16" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F9F9F9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PaginationButton">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#404B52"/>
            <Setter Property="BorderBrush" Value="#EEEEEE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinWidth" Value="22"/>
            <Setter Property="Height" Value="23"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="3.5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer x:Name="MainScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="30">
                <TextBlock Text="Quản lý bảo trì và sự cố" FontSize="24" FontWeight="Bold" Foreground="#1F384C" Margin="0,0,0,20"/>

                <Grid Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Border Background="White" CornerRadius="10" BorderBrush="#EEEEEE" BorderThickness="1" Padding="10,8">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="../../../Resources/Images/search_icon.png" Width="16" Height="16"/>
                                <TextBox x:Name="SearchTextBox" Text="Tìm kiếm..." Foreground="#B5B7C0" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" Margin="10,0,0,0" Width="150" FontSize="12" TextChanged="SearchTextBox_TextChanged" GotFocus="SearchTextBox_GotFocus" LostFocus="SearchTextBox_LostFocus"/>
                            </StackPanel>
                        </Border>
                        <Button Name="SortButton" Margin="20,0,0,0" BorderThickness="0" Background="Transparent" Cursor="Hand" Click="SortButton_Click">
                            <Border Background="White" CornerRadius="10" BorderBrush="#EEEEEE" BorderThickness="1" Padding="15,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Sắp xếp theo : " Foreground="#7E7E7E" VerticalAlignment="Center" FontSize="12"/>
                                    <TextBlock x:Name="SortButtonText" Text="Mới nhất" Foreground="#3D3C42" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/>
                                    <Path Data="M 0 0 L 4.5 4.5 L 9 0" Stroke="#7E7E7E" StrokeThickness="1.5" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                </StackPanel>
                            </Border>
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Mới nhất" Click="SortMenuItem_Click"/>
                                    <MenuItem Header="Phòng" Click="SortMenuItem_Click"/>
                                    <MenuItem Header="Ngày yêu cầu" Click="SortMenuItem_Click"/>
                                    <MenuItem Header="Ngày hoàn thành" Click="SortMenuItem_Click"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource ActionButton}" Tag="../../../Resources/Images/edit_icon.png" Margin="0,0,10,0" Click="EditButton_Click"/>
                        <Button Style="{StaticResource ActionButton}" Tag="../../../Resources/Images/delete_icon.png" Margin="0,0,10,0" Click="DeleteButton_Click"/>
                        <Button Style="{StaticResource ActionButton}" Tag="../../../Resources/Images/add_icon.png" Click="AddButton_Click"/>
                    </StackPanel>
                </Grid>

                <Border Background="White" CornerRadius="15" Padding="20">
                    <StackPanel>
                        <Grid Margin="10,0,10,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="SelectAllCheckBox" Grid.Column="0" VerticalAlignment="Center" Checked="SelectAll_Checked" Unchecked="SelectAll_Unchecked"/>
                            <TextBlock Grid.Column="1" Text="Phòng" Foreground="#B5B7C0" FontWeight="Medium" FontSize="14"/>
                            <TextBlock Grid.Column="2" Text="Người thuê" Foreground="#B5B7C0" FontWeight="Medium" FontSize="14"/>
                            <TextBlock Grid.Column="3" Text="Mô tả" Foreground="#B5B7C0" FontWeight="Medium" FontSize="14"/>
                            <TextBlock Grid.Column="4" Text="Ngày yêu cầu" Foreground="#B5B7C0" FontWeight="Medium" FontSize="14"/>
                            <TextBlock Grid.Column="5" Text="Ngày hoàn thành" Foreground="#B5B7C0" FontWeight="Medium" FontSize="14"/>
                            <TextBlock Grid.Column="6" Text="Tình trạng" Foreground="#B5B7C0" FontWeight="Medium" FontSize="14"/>
                            <TextBlock Grid.Column="7" Text="Chi phí" Foreground="#B5B7C0" FontWeight="Medium" FontSize="14"/>
                        </Grid>

                        <StackPanel x:Name="MaintenanceListDataPanel"/>
                    </StackPanel>
                </Border>

                <Grid Margin="0,20,0,0">
                    <TextBlock x:Name="PaginationInfoText" VerticalAlignment="Center" Foreground="#B5B7C0" FontSize="14" FontWeight="Medium"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="PrevPageButton" Style="{StaticResource PaginationButton}" Content="&lt;" Margin="0,0,12,0" Click="PrevPageButton_Click"/>
                        <StackPanel x:Name="PageButtonsPanel" Orientation="Horizontal"/>
                        <Button x:Name="NextPageButton" Style="{StaticResource PaginationButton}" Content=">" Margin="0,0,0,0" Click="NextPageButton_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <Border x:Name="ModalOverlay" Background="#000000" Opacity="0.3" Visibility="Collapsed"/>
        <local:EditMaintenanceView x:Name="EditMaintenanceModal" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" CloseClick="CloseEditModal_Click" SaveClick="SaveEditModal_Click"/>
        <local:AddMaintenanceView x:Name="AddMaintenanceModal" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" CloseClick="CloseAddModal_Click" ConfirmClick="AddConfirm_Click"/>
        <local:DeleteMaintenanceView x:Name="DeleteMaintenanceModal" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" ConfirmClick="DeleteConfirm_Click" CloseClick="DeleteClose_Click"/>
    </Grid>
</UserControl>
