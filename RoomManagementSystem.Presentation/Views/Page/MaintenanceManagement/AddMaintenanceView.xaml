<UserControl x:Class="RoomManagementSystem.Presentation.Views.Page.MaintenanceManagement.AddMaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="362" d:DesignWidth="535"
             FontFamily="Roboto">

    <UserControl.Resources>
        <!-- Hiệu ứng đổ bóng cho modal -->
        <DropShadowEffect x:Key="DropShadowEffect" Color="Black" Direction="315" ShadowDepth="1" BlurRadius="5.4" Opacity="0.14"/>

        <!-- Kiểu cho nút chính -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#292D32"/>
            <Setter Property="Background" Value="#F3F1FF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Kiểu cho văn bản giữ chỗ (placeholder) -->
        <Style x:Key="PlaceholderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B5B7C0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
        </Style>
    </UserControl.Resources>

    <!-- Vùng chứa chính của modal -->
    <Border Background="White" CornerRadius="14" Width="524" Height="351" Padding="38,35,38,25" Effect="{StaticResource DropShadowEffect}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Hàng 1: Tiêu đề và nút Đóng -->
            <Grid Grid.Row="0">
                <TextBlock Text="Thêm thông tin bảo trì và sự cố" FontSize="20" Foreground="#8A8A8A" VerticalAlignment="Center"/>
                <Button Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-25,-28,0"
                        Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click">
                    <Path Data="M 0,0 L 12,12 M 0,12 L 12,0" StrokeThickness="1.5">
                        <Path.Stroke>
                            <SolidColorBrush Color="Black" Opacity="0.34"/>
                        </Path.Stroke>
                    </Path>
                </Button>
            </Grid>

            <!-- Hàng 3: Form nhập liệu -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Cột trái -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Phòng" FontSize="13" Foreground="#292D32" Margin="0,0,0,4"/>
                    <Border Background="#F9FBFF" CornerRadius="9.5" Height="36" BorderBrush="#F0EEEE" BorderThickness="1">
                        <Grid>
                            <TextBlock Text="Nhập số phòng" Style="{StaticResource PlaceholderTextStyle}"/>
                            <TextBox Background="Transparent" BorderThickness="0" Padding="12,0" VerticalContentAlignment="Center"/>
                        </Grid>
                    </Border>

                    <TextBlock Text="Mô tả" FontSize="13" Foreground="#292D32" Margin="0,20,0,4"/>
                    <Border Background="#F9FBFF" CornerRadius="9.5" Height="36" BorderBrush="#F0EEEE" BorderThickness="1">
                        <Grid>
                            <TextBlock Text="Nhập mô tả" Style="{StaticResource PlaceholderTextStyle}"/>
                            <TextBox Background="Transparent" BorderThickness="0" Padding="12,0" VerticalContentAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Cột phải -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Người thuê" FontSize="13" Foreground="#292D32" Margin="0,0,0,4"/>
                    <Border Background="#F9FBFF" CornerRadius="9.5" Height="36" BorderBrush="#F0EEEE" BorderThickness="1">
                        <Grid>
                            <TextBlock Text="Lựa chọn người thuê" Style="{StaticResource PlaceholderTextStyle}"/>
                            <ComboBox Background="Transparent" BorderThickness="0" Padding="12,0" VerticalContentAlignment="Center"
                                      IsEditable="True" IsReadOnly="True">
                                <ComboBox.Resources>
                                    <!-- Ghi đè màu nền khi focus để giữ trong suốt -->
                                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="Transparent"/>
                                </ComboBox.Resources>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <TextBlock Text="Ngày yêu cầu" FontSize="13" Foreground="#292D32" Margin="0,20,0,4"/>
                    <Border Background="#F9FBFF" CornerRadius="9.5" Height="36" BorderBrush="#F0EEEE" BorderThickness="1">
                        <Grid>
                            <!-- Không cần placeholder vì đã có ngày mặc định -->
                            <DatePicker Background="Transparent" BorderThickness="0" Padding="12,0,5,0" VerticalContentAlignment="Center"
                                        SelectedDate="{x:Static sys:DateTime.Now}"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Hàng 5: Nút xác nhận -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Thêm" Width="78" Height="37" Style="{StaticResource PrimaryButtonStyle}" Click="ConfirmButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>