<Window x:Class="RoomManagementSystem.Presentation.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        MinWidth="960" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Roboto"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        UseLayoutRounding="True">

    <Window.Resources>
        
        
        
        <!-- Style cho Border chính của cửa sổ -->
        <Style TargetType="Border" x:Key="MainWindowBorderStyle">
            <Setter Property="CornerRadius" Value="10"/>
        </Style>

        <!-- Style cho các nút điều khiển cửa sổ -->
        <Style TargetType="Button" x:Key="WindowControlButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E5E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style riêng cho nút Maximize/Restore để tự đổi icon -->
        <Style TargetType="Button" x:Key="MaximizeButtonStyle" BasedOn="{StaticResource WindowControlButton}">
            <Setter Property="Content" Value="&#xE922;"/>
            <!-- Icon Maximize mặc định -->
            <Style.Triggers>
                <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                    <Setter Property="Content" Value="&#xE923;"/>
                    <!-- Icon Restore khi cửa sổ được phóng to -->
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho Nút Login -->
        <Style TargetType="Button" x:Key="LoginButtonStyle">
            <Setter Property="Background" Value="#0575E6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="59"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="3" Direction="315" Color="Black" Opacity="0.13" BlurRadius="4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" CornerRadius="29.5" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Style="{StaticResource MainWindowBorderStyle}" Background="White" ClipToBounds="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="860*"/>
                <ColumnDefinition Width="580*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Border CornerRadius="10,0,0,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                            <GradientStop Color="#0575E6" Offset="0.0"/>
                            <GradientStop Color="#02298A" Offset="0.848"/>
                            <GradientStop Color="#021B79" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <Canvas>
                    <Ellipse Width="556" Height="556" Stroke="#0575E6" StrokeThickness="1" Opacity="0.3" Canvas.Left="-160" Canvas.Bottom="-350"/>
                    <Ellipse Width="556" Height="556" Stroke="#0575E6" StrokeThickness="1" Opacity="0.3" Canvas.Left="-250" Canvas.Bottom="-300"/>
                </Canvas>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Margin="80,0,80,0">
                    <TextBlock Text="Plumeria Family" FontSize="40" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>
                    <TextBlock Text="Giải pháp số hóa quản lý cho thuê phòng hiệu quả" FontSize="18" FontWeight="Medium" Foreground="White" Margin="0,15,0,0" TextWrapping="Wrap" Opacity="0.9"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0">
                    <Button Content="&#xE921;" Style="{StaticResource WindowControlButton}" Click="MinimizeButton_Click"/>
                    <Button Style="{StaticResource MaximizeButtonStyle}" Click="MaximizeButton_Click"/>
                    <Button Content="&#xE8BB;" Style="{StaticResource WindowControlButton}" Click="CloseButton_Click"/>
                </StackPanel>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="60,0,60,0">
                    <TextBlock Text="Chào mừng bạn đã trở lại" FontSize="26" FontWeight="Bold" Foreground="#333333" HorizontalAlignment="Center"/>
                    <TextBlock Text="Bắt đầu phiên làm việc của bạn" FontSize="18" FontWeight="Normal" Foreground="#333333" Margin="0,5,0,30" HorizontalAlignment="Center"/>

                    <Border Background="#F0EDFF" CornerRadius="26" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,16">
                        <Grid Height="52" Margin="26,0,26,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="pack://application:,,,/Resources/Images/email_icon.png" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center" Opacity="0.3"/>
                            <TextBox Grid.Column="1"
                                     x:Name="EmailInput"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VerticalAlignment="Center"
                                     FontSize="14"
                                     Foreground="Black"
                                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />


                            <TextBlock Grid.Column="1" Text="Địa chỉ Email" Foreground="#333333" IsHitTestVisible="False" VerticalAlignment="Center" FontSize="14" Opacity="0.5">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=EmailInput}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>


                    <Border Background="#F0EDFF" CornerRadius="26" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,10">
                        <Grid Height="52" Margin="26,0,26,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="pack://application:,,,/Resources/Images/password_icon.png" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center" Opacity="0.3"/>
                            <PasswordBox Grid.Column="1" x:Name="PasswordInput" PasswordChanged="PasswordInput_PasswordChanged" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="14" Foreground="Black"/>
                            <TextBlock Grid.Column="1"
                                       x:Name="PlaceholderText"
                                       Text="Mật khẩu"
                                       Foreground="#333333"
                                       IsHitTestVisible="False"
                                       VerticalAlignment="Center"
                                       FontSize="14"
                                       Opacity="0.5"/>


                        </Grid>
                    </Border>

                    <Button
                        Content="Quên mật khẩu"
                        Background="Transparent"
                        Foreground="#0575E6"
                        Opacity="0.7"
                        FontSize="14"
                        Margin="0,0,0,25"
                        BorderThickness="0"
                        Cursor="Hand"
                        HorizontalAlignment="Right"
                        Command="{Binding ForgotPasswordCommand}"/>


                    <Button
                        Content="Đăng nhập"
                        Style="{StaticResource LoginButtonStyle}"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=PasswordInput}"
                        Margin="0,0,0,25"/>

                    <TextBlock HorizontalAlignment="Center">
                        <Run Text="Bạn chưa có tài khoản? " Foreground="Black"/>
                        <Hyperlink Foreground="#0575E6" Cursor="Hand" Click="RegisterHyperlink_Click">
                            <Run Text="Vui lòng liên hệ để được hỗ trợ"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>