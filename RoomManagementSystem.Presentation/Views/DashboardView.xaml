<UserControl x:Class="RoomManagementSystem.Presentation.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1150"
             FontFamily="Poppins"
             Background="#F5F6FA">

    <!-- ... (Resources của bạn vẫn giữ nguyên) ... -->
    <UserControl.Resources>
        <!-- Style cho tiêu đề của mỗi Card -->
        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#1F384C"/>
        </Style>

        <!-- Style cho tiêu đề phụ/mô tả -->
        <Style x:Key="CardSubtitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#8A8A8A"/>
        </Style>

        <!-- Style cho các con số lớn -->
        <Style x:Key="LargeNumber" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- Style cho nút "Xem chi tiết" -->
        <Style x:Key="ViewDetailsButton" TargetType="Button">
            <Setter Property="Foreground" Value="#5A6ACF"/>
            <Setter Property="BorderBrush" Value="#DDE4F0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="2"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F9FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E8F0FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="380"/>
                <ColumnDefinition Width="2*" MinWidth="350"/>
                <ColumnDefinition Width="2.2*" MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <!-- Hàng 0: Tiêu đề "Tổng quan" -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Tổng quan" FontSize="18" FontWeight="Medium" Foreground="#1F384C" Margin="0,0,0,16"/>

            <!-- Hàng 1: Doanh thu và Tình trạng phòng -->
            <!-- Card Doanh thu tháng -->
            <materialDesign:Card Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Padding="20" Margin="0,0,12,12" UniformCornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Doanh thu tháng" Style="{StaticResource CardTitle}"/>
                            <TextBlock Text="380 000 000 VND" Style="{StaticResource LargeNumber}" Margin="0,5,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <materialDesign:PackIcon Kind="ArrowUp" Foreground="#149D52" VerticalAlignment="Center"/>
                                <TextBlock Text="2.1%" FontWeight="SemiBold" Foreground="#149D52" Margin="4,0,0,0"/>
                                <TextBlock Text=" so với tháng trước" Foreground="#737B8B" Margin="5,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                        <Button Content="Xem chi tiết" Style="{StaticResource ViewDetailsButton}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    </Grid>

                    <TextBlock Grid.Row="1" Text="Cho thuê từ tháng 04, 2025" Style="{StaticResource CardSubtitle}" Margin="0,0,0,16"/>

                    <!-- Biểu đồ cột Doanh thu -->
                    <scottplot:WpfPlot x:Name="RevenueChart" Grid.Row="2" MinHeight="180"/>
                </Grid>
            </materialDesign:Card>

            <!-- Card Tình trạng phòng -->
            <materialDesign:Card Grid.Row="1" Grid.Column="2" Padding="20" Margin="12,0,0,12" UniformCornerRadius="10">
                <StackPanel>
                    <!-- Header -->
                    <Grid Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Tình trạng phòng" Style="{StaticResource CardTitle}"/>
                            <TextBlock Text="Từ 1-30 Thg 4, 2025" Style="{StaticResource CardSubtitle}" Margin="0,5,0,0"/>
                        </StackPanel>
                        <Button Content="Xem chi tiết" Style="{StaticResource ViewDetailsButton}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    </Grid>

                    <!-- Biểu đồ Donut và Chú thích -->
                    <Grid>
                        <!-- Biểu đồ Donut Tình trạng phòng -->
                        <scottplot:WpfPlot x:Name="RoomStatusChart" MinHeight="260"/>

                        <!-- Chú thích -->
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
                            <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                                <Ellipse Width="9" Height="9" Fill="#5A6ACF" VerticalAlignment="Center"/>
                                <TextBlock Text="Đang thuê (40%)" Margin="8,0,0,0" Foreground="#121212" Opacity="0.7"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                                <Ellipse Width="9" Height="9" Fill="#8593ED" VerticalAlignment="Center"/>
                                <TextBlock Text="Dự kiến (32%)" Margin="8,0,0,0" Foreground="#121212" Opacity="0.7"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="9" Height="9" Fill="#C7CEFF" VerticalAlignment="Center"/>
                                <TextBlock Text="Trống (28%)" Margin="8,0,0,0" Foreground="#121212" Opacity="0.7"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </materialDesign:Card>

            <!-- ... (Phần còn lại của XAML không thay đổi) ... -->
            <!-- Hàng 2: Chi phí, Công nợ và Lợi nhuận -->
            <!-- Card Chi phí tháng -->
            <materialDesign:Card Grid.Row="2" Grid.Column="0" Padding="20" Margin="0,12,12,0" UniformCornerRadius="10">
                <StackPanel>
                    <TextBlock Text="Chi phí tháng" Style="{StaticResource CardTitle}"/>
                    <TextBlock Text="Bao gồm chi phí điện, nước, sửa chữa" Style="{StaticResource CardSubtitle}" Margin="0,5,0,20"/>

                    <!-- Các vòng tròn chi phí -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <Grid Width="60" Height="60">
                                <Ellipse Fill="#F5F9FF" Stroke="#6463D6" StrokeThickness="6"/>
                            </Grid>
                            <TextBlock Text="44,3tr" HorizontalAlignment="Center" FontWeight="SemiBold" Margin="0,8,0,0"/>
                            <TextBlock Text="Điện" HorizontalAlignment="Center" Style="{StaticResource CardSubtitle}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <Grid Width="80" Height="80">
                                <Ellipse Fill="#F5F9FF" Stroke="#F99C30" StrokeThickness="6"/>
                            </Grid>
                            <TextBlock Text="72,4tr" HorizontalAlignment="Center" FontWeight="SemiBold" Margin="0,8,0,0"/>
                            <TextBlock Text="Nước" HorizontalAlignment="Center" Style="{StaticResource CardSubtitle}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <Grid Width="70" Height="70">
                                <Ellipse Fill="#F5F9FF" Stroke="#2FBFDE" StrokeThickness="6"/>
                            </Grid>
                            <TextBlock Text="52,3tr" HorizontalAlignment="Center" FontWeight="SemiBold" Margin="0,8,0,0"/>
                            <TextBlock Text="Sửa chữa" HorizontalAlignment="Center" Style="{StaticResource CardSubtitle}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </materialDesign:Card>

            <!-- Card Công nợ -->
            <materialDesign:Card Grid.Row="2" Grid.Column="1" Padding="20" Margin="12,12,12,0" UniformCornerRadius="10">
                <StackPanel>
                    <TextBlock Text="Công nợ" Style="{StaticResource CardTitle}"/>
                    <TextBlock Text="Người thuê nợ phí chưa thanh toán" Style="{StaticResource CardSubtitle}" Margin="0,5,0,16"/>
                    <ListView>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0,8"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <!-- Lặp qua danh sách công nợ ở đây -->
                        <ListViewItem>
                            <Grid>
                                <TextBlock Text="Lê Công Bảo" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <TextBlock Text="2tr VND" HorizontalAlignment="Right" VerticalAlignment="Center" Opacity="0.7"/>
                            </Grid>
                        </ListViewItem>
                        <ListViewItem>
                            <Grid>
                                <TextBlock Text="Lê Công Bảo" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <TextBlock Text="3tr VND" HorizontalAlignment="Right" VerticalAlignment="Center" Opacity="0.7"/>
                            </Grid>
                        </ListViewItem>
                        <ListViewItem>
                            <Grid>
                                <TextBlock Text="Trần Thanh Nhã" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <TextBlock Text="4,8tr VND" HorizontalAlignment="Right" VerticalAlignment="Center" Opacity="0.7"/>
                            </Grid>
                        </ListViewItem>
                        <ListViewItem>
                            <Grid>
                                <TextBlock Text="Trần Thanh Nhã" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <TextBlock Text="1,2tr VND" HorizontalAlignment="Right" VerticalAlignment="Center" Opacity="0.7"/>
                            </Grid>
                        </ListViewItem>
                    </ListView>
                </StackPanel>
            </materialDesign:Card>

            <!-- Card Lợi nhuận -->
            <materialDesign:Card Grid.Row="2" Grid.Column="2" Padding="20" Margin="12,12,0,0" UniformCornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Lợi nhuận" Style="{StaticResource CardTitle}"/>
                            <TextBlock Text="132 000 000 VND" Style="{StaticResource LargeNumber}" Margin="0,5,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <materialDesign:PackIcon Kind="ArrowDown" Foreground="#F2383A" VerticalAlignment="Center"/>
                                <TextBlock Text="2.1%" FontWeight="SemiBold" Foreground="#F2383A" Margin="4,0,0,0"/>
                                <TextBlock Text=" so với tháng trước" Foreground="#737B8B" Margin="5,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                        <Button Content="View Report" Style="{StaticResource ViewDetailsButton}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    </Grid>

                    <TextBlock Grid.Row="1" Text="Thống kê từ tháng 1 - 6, 2025" Style="{StaticResource CardSubtitle}" Margin="0,0,0,16"/>

                    <!-- Biểu đồ đường Lợi nhuận -->
                    <scottplot:WpfPlot x:Name="ProfitChart" Grid.Row="2" MinHeight="100"/>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </ScrollViewer>
</UserControl>