using RoomManagementSystem.Presentation.Views.Windows;
using System.Windows;

namespace RoomManagementSystem.Presentation
{
    public partial class App : Application
    {
        //protected override void OnStartup(StartupEventArgs e)
        //{
        //    base.OnStartup(e);

        //    LoginWindow loginWindow = new LoginWindow();
        //    // Hiển thị cửa sổ đăng nhập như một hộp thoại
        //    bool? dialogResult = loginWindow.ShowDialog();

        //    // Nếu đăng nhập thành công (dialogResult là true)
        //    if (dialogResult.HasValue && dialogResult.Value)
        //    {
        //        // Thì mới tạo và hiển thị cửa sổ chính
        //        MainWindow mainWindow = new MainWindow();
        //        mainWindow.Show();
        //    }
        //    else
        //    {
        //        // Nếu không đăng nhập thành công thì tắt ứng dụng
        //        Shutdown();
        //    }
        //}
    }
}